name: build-and-test

on:
  push:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build and test
        run: |
          docker build -t smartev-test .
          docker run smartev-test dotnet test --no-build